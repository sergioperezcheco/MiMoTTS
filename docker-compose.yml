services:
  mimotts:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - MIMO_API_KEY=${MIMO_API_KEY:-${VITE_MIMO_API_KEY}}
      - MIMO_BASE_URL=${MIMO_BASE_URL:-${VITE_MIMO_BASE_URL:-https://token-plan-sgp.xiaomimimo.com/v1}}
    restart: unless-stopped
